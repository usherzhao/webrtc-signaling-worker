name = "webrtc-signaling-server"
main = "src/index.js"
compatibility_date = "2024-02-28"

# 绑定 Durable Object 命名空间
[[durable_objects.bindings]]
name = "SIGNALING_DO"
class_name = "SignalingDO"

# 声明 DO 的迁移记录（注意这里改成了 new_sqlite_classes）
[[migrations]]
tag = "v1"
new_sqlite_classes = ["SignalingDO"]
